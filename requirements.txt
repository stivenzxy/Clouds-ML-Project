# Dependencias mínimas para inferencia + demo Streamlit (deploy).
# Para entrenar / reproducir experimentos, usar requirements-dev.txt.
#
# Instalación local:
#   pip install -r requirements.txt

# PyTorch CPU (versión liviana para servidores sin GPU como Streamlit Cloud)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0
torchvision==0.17.0

# Arquitecturas preentrenadas (DenseNet121, MobileNetV3)
timm>=0.9

# Procesamiento de imágenes (la app solo usa PIL, no OpenCV)
pillow>=10.0

# Numérica
numpy>=1.24
pandas>=2.0

# Interfaz
streamlit>=1.30
