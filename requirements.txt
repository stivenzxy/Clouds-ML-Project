# Dependencias mínimas para inferencia + demo Streamlit (deploy).
# Para entrenar / reproducir experimentos, usar requirements-dev.txt.
#
# Instalación local:
#   pip install -r requirements.txt
#
# NOTA: Streamlit Cloud usa Python 3.14 por defecto, así que NO pineamos
# versiones específicas de torch — dejamos que pip resuelva el wheel CPU
# más nuevo compatible con la versión de Python del entorno.

# PyTorch CPU (wheels livianos, sin CUDA)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# Arquitecturas preentrenadas (DenseNet121, MobileNetV3)
timm>=0.9

# Procesamiento de imágenes (la app solo usa PIL, no OpenCV)
pillow>=10.0

# Numérica
numpy>=1.24
pandas>=2.0

# Interfaz
streamlit>=1.30
