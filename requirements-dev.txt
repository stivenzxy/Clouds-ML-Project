# Dependencias del proyecto.
# Instalación:
#   pip install -r requirements.txt
#
# Para inferencia en laptop (punto F, sin GPU), usar PyTorch CPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt --no-deps  (si ya está torch instalado)

# --- Deep learning ---
torch>=2.0
torchvision>=0.15
timm>=0.9                       # arquitecturas preentrenadas (DenseNet121, MobileNetV3)

# --- Procesamiento de imágenes ---
albumentations>=1.3             # augmentations para train
opencv-python>=4.7              # lectura/decodificación de imágenes

# --- Datos / numérica ---
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
iterative-stratification>=0.1.7  # split estratificado multi-etiqueta (módulo: iterstrat)

# --- Utilidades / logging ---
tqdm>=4.65
wandb>=0.16                     # tracking de experimentos (modo offline configurado)
matplotlib>=3.7                 # figuras del paper y matrices de confusión

# --- Interfaz demo (punto F) ---
streamlit>=1.30                 # demo web local
pillow>=10.0                    # PIL para manipulación de imágenes en Streamlit
